# Verification — v0.10.2

Required gates:

- focused UI hotfix fixtures;
- full Node regression;
- validator and syntax checks;
- packaging;
- rerun from the frozen v0.10.2 source ZIP.

The release claim is source/package only until Desktop Chrome verifies:

- header and document title show v0.10.2;
- the side panel renders without `windowContext` ReferenceError;
- existing v0.10.1 lifecycle behavior remains operational.
