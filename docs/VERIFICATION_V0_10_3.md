# Verification — v0.10.3

This document is populated by the build verification run.

Required gates:

- focused v0.10.3 fixtures;
- full regression;
- validator;
- syntax checks;
- package build;
- final rerun from the frozen v0.10.3 source ZIP;
- ZIP CRC/path/symlink/encryption checks.

Runtime claims require separate Desktop Chrome acceptance.
