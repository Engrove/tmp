# Desktop Chrome acceptance — v0.10.2

1. Load the v0.10.2 browser directory unpacked.
2. Verify `chrome://extensions` reports version 0.10.2.
3. Open the side panel and verify both the document title and header show v0.10.2.
4. Open each Mission Control view and refresh state repeatedly.
5. Verify the extension error page has no `windowContext is not defined` entry.
6. Link a supported ChatGPT tab and start a continuation mission.
7. Verify Session Capture, Nano readiness and EIC-AA/5 behavior remain unchanged.
8. Export state and verify `appVersion` is 0.10.2 while config/runtime remain v12 and export remains v19.
