# EIC Autonom Agent v0.11.9

- Preserves the v0.11.8 session-init baseline-correction generation fence.
- Preserves the v0.11.7 typed schema-bound Nano JSON-completeness postcondition and single format-repair semantics.
- Preserves the v0.11.6 6,000-character advisory threshold and schema-derived hard output ceiling.
- Fixes side-band control-response identity rearm: fresh `responseIdentity`, `responseHash`, trailer `turnId`, and side-band turn ids do not by themselves create a new material decision input.
- Keys side-band material identity to the stable run/task/init/material-effect anchor, owner-controlled `materialControlGeneration`, and substantive target control semantics.
- Keeps non-side-band assistant response identity material.
- Keeps accepted Core Surface Review generation changes material so one intended local reanalysis is still allowed.
- Keeps substantive target `next` / actor / autonomy / completion-state changes material.
- Preserves v0.11.5 actor/capability separation and terminal Nano diagnostic one-per-generation suppression.
